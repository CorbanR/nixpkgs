{
    "name": "cf-worker",
    "version": "1.0.0",
    "description": "proxy requests for gitlfs assets",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'"
    },
    "author": "Corban Raun <corban@raunco.co>",
    "license": "ISC",
    "devDependencies": {
        "prettier": "^1.17.0"
    },
    "dependencies": {
        "serverless-cloudflare-workers": "^1.2.0"
    }
}
